/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package hashmap_java;

/**
 *
 * @author Pycoder
 */
public class HashMap_Java {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
       
        
        /*
        It provides the basic implementation of Map interface of Java. 
        It stores the data in (Key, Value) pairs. 
        To access a value one must know its key.
        HashSet also uses HashMap internally. 
        It internally uses a link list to store key-value pairs in HashSet.
        ashMap extends an abstract class AbstractMap
        It also implements Cloneable and Serializable interface.
        HashMap doesn’t allow duplicate keys but allows duplicate values.
        */
        
        
        
        /***
         * 
         * HashMap allows null key also but only once and multiple null values.
         * Performance of HashMap depends on 2 parameters:
         *      
         * 
         * Initial Capacity
           Load Factor
         */
    }
    
}
